import { Characters, WinAnsiMap } from '../../src/constants';

describe('constants', () => {

  it('Characters is defined', () => {
    expect(Characters).toBeDefined();
  });

  it('WinAnsiMap is defined', () => {
    expect(WinAnsiMap).toBeDefined();
  });

});
